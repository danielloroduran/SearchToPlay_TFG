class FechaLanzamiento{
    
    
  /*  static List<FechaLanzmiento> listFromMapList(List<dynamic> mapList) {
    if (mapList == null) {
      return new List<FechaLanzmiento>();
    }

    return mapList.map((m) => fromMap(m)).toList();
  }*/
}